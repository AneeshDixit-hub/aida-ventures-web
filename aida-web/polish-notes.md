# Polish Notes

## Screenshot Observations
- Hero looks good: dark navy bg, cityscape image overlay, white text, teal eyebrow
- Navigation: AIDA wordmark not visible (needs to be white on dark hero, then dark on scroll)
- "I'm an LP" CTA not visible in nav (may be hidden behind dark bg)
- Metrics strip partially visible at bottom — looks good
- Need to verify: nav wordmark visibility on hero, then color change on scroll
- Need to check all pages render correctly
- Need to ensure mobile responsiveness

## Fixes Needed
1. Nav wordmark and links need to be white when over dark hero, then dark on scroll
2. Verify all internal pages
3. Check mobile menu
